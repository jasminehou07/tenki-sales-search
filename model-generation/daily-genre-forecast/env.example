# Absolute directory containing events/, genre-sales*/, and genre-ranking*/.
# Local numbered layout example:
TENKI_RAW_DATA_DIR=/path/to/TENKI/data files

# Consolidated server layout example:
# TENKI_RAW_DATA_DIR=/root
