numpy>=1.26
pandas>=2.1
pyarrow>=14
psycopg[binary]>=3.1
scikit-learn>=1.3
# Optional at runtime because the rank model falls back to scikit-learn when
# XGBoost cannot be imported; install it to reproduce the preferred model path.
xgboost>=2.0
